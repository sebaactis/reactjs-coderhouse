import NavBar from "../visual/NavBar";


const Footer = () => {
    return (
        <footer className="footer">
            <NavBar isInHeader={false}/>
        </footer>
    );
};

export default Footer;